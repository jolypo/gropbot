# Secrets Setup

The real Telegram bot tokens and SAHMK API key are stored in `.env`.
The distributable ZIP intentionally excludes `.env`.

For security, rotate/revoke these credentials if they have been exposed outside a trusted environment.
